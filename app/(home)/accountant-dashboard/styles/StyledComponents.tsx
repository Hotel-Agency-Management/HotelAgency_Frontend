import { styled } from '@mui/material/styles'
import { Stack } from '@mui/material'

export const LoadingBox = styled(Stack)({
  alignItems: 'center',
  justifyContent: 'center',
  minHeight: 240,
})
